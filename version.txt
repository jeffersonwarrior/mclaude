Mclaude V1.2.2 Minor Bug Fixes

Forked from Synclaude by jeffersonwarrior (https://github.com/jeffersonwarrior)
Original Synclaude Author: parnexcodes (https://github.com/parnexcodes)

V1.2.2 Changes (Minor Bug Fixes):
- Fixed React/Ink Text component error with space-only Text components
- Resolved empty string validation errors in ModelSelector UI
- Updated version to 1.2.2 across all configuration files

V1.2.1 Changes (Minor Bug Fixes):
- Fixed error message to suggest 'mclaude setup' when no API key found (not 'mclaude models')
- Updated Synthetic API key validation to accept 'syn_' prefix instead of 'sk_' prefix
- Added conditional provider testing to skip testing when API key is not provided
- Improved error trapping to hide full stack traces on authentication failures
- Enhanced Synthetic API key format validation

V1.2 Changes (Bug Fixes):
- Fixed version display to show correct v1.2.0 instead of v1.0.0
- Added early termination when API authentication fails during setup
- Improved error handling to trap full error logs on authentication failure
- Fixed .env detection logic to properly handle new files
- Resolved React hook call error in model selection UI
- Enhanced setup process to stop on critical failures
- Updated User-Agent headers to correct version
- Improved overall error messaging and user experience

V1.1 Bug Fixes:
- Fixed various setup and configuration issues

V1.0 Initial Release Changes:
- Forked from synclaude to create mclaude
- Updated branding and command references from synclaude to mclaude
- Maintained all original functionality with new project identity
- Complete command line interface now responds to 'mclaude' command
- All configuration and cache paths updated to ~/.config/mclaude/
- Installation scripts updated for new mclaude branding
- Ready for initial release with full feature parity

This fork provides the same model selection and Synthetic AI integration capabilities as synclaude while establishing a new project identity and addressing critical bugs.